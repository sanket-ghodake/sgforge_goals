# Submodule Core Rules

- **Execution**: Run commands using `./run.sh` or local portable binaries (`./portables/bin/*`).
- **Paths**: Never reference `../../apps/src/*` or parent monorepo folders.
- **Formatting**: Keep source files <= 300 lines.
