# App Template - Low-Level Requirements (`docs/llr/`)

This directory houses Low-Level Requirements (LLRs) for new apps scaffolded from this template.
Each function implemented in the new micro-app links back to an LLR file created here.
