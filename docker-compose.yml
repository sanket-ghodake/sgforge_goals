# ==============================================================================
# Standalone Forge App Compose: Quick-Launch Root Compose (Air-Gapped by Default)
# Invariant: internal: true -> ZERO outbound internet egress. Inbound via proxy.
#
# USAGE MATRIX (choose ONE at a time to avoid container_name conflicts):
#   Root (this file)         ./run.sh compose up -d           → container: ag-app-goals-local
#   Dev  (docker/dev/)       cd docker/dev && docker compose  → container: ag-app-goals-dev
#   Prod (docker/prod/)      cd docker/prod && docker compose → container: ag-app-goals-prod
# ==============================================================================
name: forge-app-goals-local

networks:
  # 1. Isolated Application Network (Strict Air-Gap: No default gateway to internet)
  goals-airgap-net:
    driver: bridge
    internal: true

  # 2. Reverse-Proxy Gateway Mesh (Inbound routing from Caddy proxy only)
  forge-gateway-net:
    name: ${FORGE_APPS_NETWORK:-ag_forge_apps_net}
    external: true

services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: ${CONTAINER_PREFIX:-ag}-app-goals-local
    ports:
      - "${PORT:-8090}:${PORT:-8090}"
    env_file:
      - path: .env
        required: false
    environment:
      - PORT=${PORT:-8090}
      - NODE_ENV=${NODE_ENV:-development}
    command: ["bun", "--watch", "src/server.ts"]
    volumes:
      - ./src:/app/src
      - ./data:/app/data
      - ./logs:/app/logs
    networks:
      goals-airgap-net:
      forge-gateway-net:
        aliases:
          - app-goals-local
    restart: unless-stopped
    stop_grace_period: 10s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:${PORT:-8089}/health || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 5s
