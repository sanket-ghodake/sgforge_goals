# Goals Micro-App - Frontend Layer (`src/frontend/`)

This directory houses client-facing user interfaces and view rendering components:

- [`views/`](./views): Single Page Application (SPA) view renders (Cockpit, Board Canvas, Manager Review Center, Hall of Impact, and Master Layout).
